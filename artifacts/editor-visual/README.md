# Editor visual verification

Playwright regenerates these retained Chromium artifacts from the rendered documentation mock and production Editor at identical viewports. Each state includes the reference, production, pixel difference, side-by-side review, and difference summary.

The required states cover Structured content, collapsed Source, Source with Quick Add, Layout with hovered bounds, expanded filtered diagnostics, and desktop/two-pane/single-column responsive layouts. The HTML Playwright report also carries the corresponding per-browser attachments.

Production-only evidence additionally retains the default-off Developer control and enabled Editor-hub state for Open Project Folder, the modern collapsed and expanded Find states (rounded entries, inlaid neutral toggles, arrow/count ordering, and checkbox-disclosed inline replacement controls), exact same-line embedded-field syntax underlining with expanded diagnostics and last-valid preview, the dark Structured precise-control state (shared integer steppers and checkbox targeting), the folded Source state with its single gutter control, the Editor-hub three-line description with its full-text hover panel and fixed footer clearance, compact `⌘` shortcut hints in Quick Add and Settings, All Completions layered over an open Quick Add palette, an existing empty required field offered as a completion target, Show Bounds off/on states proving that the authored preview surface remains content-sized, the shared project-deletion confirmation in default and contrasting accents, and the Files workflow with Source as its only editing mode.
